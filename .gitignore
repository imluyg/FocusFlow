/target
**/*.rs.bk

# 运行时生成物（数据/日志/备份/配置/插件）——任意层级
**/data/
**/logs/
**/backup/
/plugins/
**/config.ini
**/config.ini.corrupt-*
**/window_state.ini
**/dist/

# `focusflow-cli --export` 写到**当前目录**，在仓库根跑一次就会产出一份完整按键
# 统计（键名 + 次数）。文件名固定、不含随机后缀，一旦 `git add -A` 就被静默收录。
**/focusflow_export.csv
**/focusflow_export.html

# .scratch/package.sh 每次打包都在**仓库根**重写的构建日志（挪进 .scratch 也没用，
# 它下次打包还会写回来，见交接文档 §八·3）
/pkg.txt

# Tauri 构建产物与打包目录（含用户数据，不入库）
/dist-tauri/
/desktop/gen/
/desktop/target/

# AI 助手 / 编辑器的工作目录与说明文件：属个人环境，不入库
.scratch/
.zcode/
.claude/
.cursor/
.workbuddy/
.dsh/
.opencode/
.windsurf/
.aider*
CLAUDE.md
AGENTS.md

# ===== 设计文档 =====
# 本地保留（设计小节、台账、验收清单、体检报告），一律不入库。
docs/
